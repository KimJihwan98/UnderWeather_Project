<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.ssafit.model.dao.SportDao">

    
    <select id="selectAll" parameterType="string" resultType="Sport">
        select * from Sport where spName=#{spName}            
    </select>
    
    <select id="selectOne" parameterType="string" resultType="Sport">
        select * from Sport where spName=#{spName}            
    </select>
    

</mapper>